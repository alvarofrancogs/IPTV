---
services:
  iptvnator:
    container_name: iptvnator-firetv
    image: iptvnator-firetv:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    ports:
      - "4334:80"
    environment:
      BACKEND_URL: /api
      CLIENT_URL: http://localhost:4334
      PORT: "3000"
      IPTVNATOR_PROXY_ALLOW_PRIVATE_NETWORKS: "0"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/api/health >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
